#include<stdio.h>
int main(){
	int i,j;
	//uning , we can initialize ,check condition and increment multiple exeprision statements like this...
	//we can also use Logical opretator(&&,||,!) in to check conditions..ok
	for(i=1,j=10; i<10,j>0; i++,j--){
		printf("%d\t%d\n",i,j);
	}
	
	return 0;
}
