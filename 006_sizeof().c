#include<stdio.h>
int main()
{
	int l=10;
	/* sizeof() is used with the data types such as int, float, char… etc it simply returns the amount of memory is allocated to that 
	data types.*/
	printf("int is %d bit",sizeof(l));
	return 0;
}
