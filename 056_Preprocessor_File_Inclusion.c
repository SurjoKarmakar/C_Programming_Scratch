#include<stdio.h>  //search for standard directory 
#include "mycal.c"  //it search the current directory...

int main(){
	int add2no = add(4,6);
	printf("sum of 2 no is : %d\n",add2no);
	return 0;
}